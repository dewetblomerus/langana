<ul class='text-center'>
  <h3>References from people I've worked for</h3>
  <% @worker.work_references.each do |reference| %>
    <li>
      <div>
        <small>
          Reviewed by: <%= link_to reference.employer.first_name, reference.employer %>
        </small>
      <div id="reference_title">
        <%= reference.work.capitalize %>
      </div>
      <div id="reference_body">
        <%= reference.comment %>
      </div>
      <div>
        Reviewed on
        <%= reference.created_at.strftime("%d %b. %Y") %>
      </div>
    </li>
  <% end %>
</ul>
